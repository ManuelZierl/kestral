source "https://rubygems.org"

# Local preview of the Just the Docs site: `bundle install && bundle exec jekyll serve`.
# GitHub Pages resolves the theme through `remote_theme` and does not use this file.
gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.8"
gem "jekyll-remote-theme"
